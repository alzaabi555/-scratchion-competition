# مسابقة سكراتشيون - الإبداع والبرمجة للمستقبل

موقع فخم وحديث لمسابقة سكراتشيون التفاعلية التي تهدف إلى تنمية مهارات البرمجة والتفكير الإبداعي لدى طلاب الصف الثالث.

## 🎯 أهداف المسابقة

- نشر ثقافة البرمجة والتفكير المنطقي بين طلاب المدارس
- دعم التحول الرقمي في المؤسسات التعليمية
- تعزيز استخدام تطبيق Scratch كأداة تعليمية
- اكتشاف وتنمية المواهب البرمجية المبكرة
- تنمية مهارات التفكير المنطقي وحل المشكلات
- بناء ثقة الطلاب بقدراتهم على إنتاج مشاريع تقنية

## 🚀 المميزات

- **تصميم فخم وحديث** - واجهة مستخدم احترافية بألوان نيون حيوية
- **رسومات كرتونية ثلاثية الأبعاد** - شخصيات بالزي العماني التقليدي
- **فيديوهات تفاعلية** - تشغيل تلقائي عند التمرير على الصفحة
- **نموذج تسجيل بسيط** - تسجيل سهل وسريع للمشاركين
- **نظام إرسال بريد إلكتروني** - إشعارات فورية لمشرف المسابقة
- **قاعدة بيانات متقدمة** - حفظ جميع التسجيلات والبيانات
- **لوحة تحكم إدارية** - عرض وإدارة التسجيلات مع تصدير البيانات
- **تصميم استجابي** - يعمل بسلاسة على جميع الأجهزة

## 📋 المتطلبات

- Node.js 22.13.0 أو أحدث
- pnpm 10.4.1 أو أحدث
- MySQL/TiDB لقاعدة البيانات

## 🛠️ التثبيت والتشغيل

### 1. استنساخ المستودع
```bash
git clone https://github.com/alzaabi555/scratchion-competition.git
cd scratchion-competition
```

### 2. تثبيت المكتبات
```bash
pnpm install
```

### 3. إعداد متغيرات البيئة
انسخ ملف `.env.example` إلى `.env` وأدخل بيانات الاتصال بقاعدة البيانات والخدمات الأخرى:
```bash
cp .env.example .env
```

### 4. تطبيق هجرات قاعدة البيانات
```bash
pnpm db:push
```

### 5. تشغيل خادم التطوير
```bash
pnpm dev
```

سيتم فتح الموقع على `http://localhost:3000`

## 📁 هيكل المشروع

```
scratchion-competition/
├── client/                    # كود الواجهة الأمامية (React)
│   ├── src/
│   │   ├── pages/            # صفحات الموقع
│   │   ├── components/       # مكونات React
│   │   ├── lib/              # مكتبات مساعدة
│   │   └── index.css         # أنماط عام
│   └── index.html
├── server/                    # كود الخادم الخلفي (Express)
│   ├── routers.ts            # تعريفات API
│   ├── db.ts                 # استعلامات قاعدة البيانات
│   └── _core/                # ملفات النظام الأساسية
├── drizzle/                   # إدارة قاعدة البيانات
│   ├── schema.ts             # تعريف الجداول
│   └── migrations/           # هجرات قاعدة البيانات
├── shared/                    # ملفات مشتركة بين الأمام والخلف
├── package.json              # إدارة المكتبات
├── vite.config.ts            # إعدادات Vite
└── README.md                 # هذا الملف
```

## 🔧 الأوامر المتاحة

```bash
# تشغيل خادم التطوير
pnpm dev

# بناء المشروع للإنتاج
pnpm build

# تشغيل الموقع المبني
pnpm start

# تشغيل الاختبارات
pnpm test

# تطبيق هجرات قاعدة البيانات
pnpm db:push

# التحقق من أخطاء TypeScript
pnpm check

# تنسيق الكود
pnpm format
```

## 📧 نظام البريد الإلكتروني

عند تسجيل أي طالب، يتم إرسال بريد إلكتروني فوراً إلى بريد مشرف المسابقة يحتوي على:
- اسم المدرسة
- اسم الطالب
- الصف
- وقت التسجيل

## 🗄️ قاعدة البيانات

### جدول التسجيلات (registrations)
| الحقل | النوع | الوصف |
|------|------|-------|
| id | INT | معرف فريد |
| schoolName | VARCHAR | اسم المدرسة |
| studentName | VARCHAR | اسم الطالب |
| grade | ENUM | الصف (الصف الثالث) |
| createdAt | TIMESTAMP | وقت التسجيل |

## 👥 الفريق

- **المطور:** Manus AI
- **المشرف:** umsufyan2008@gmail.com

## 📄 الترخيص

هذا المشروع مرخص تحت MIT License

## 📞 التواصل

للأسئلة والاستفسارات، يرجى التواصل عبر البريد الإلكتروني: umsufyan2008@gmail.com

---

**تم إنشاء هذا الموقع بواسطة Manus AI** ✨
